# Variáveis de Ambiente - Bolão Privado
# INSTRUÇÕES: Copie este arquivo para .env e preencha com seus valores reais
# ⚠️  IMPORTANTE: NUNCA committe o arquivo .env - ele já está no .gitignore

# ========================================
# SERVIDOR
# ========================================
# Ambiente: production, development, test
NODE_ENV=development

# Porta que o servidor vai rodar
# Default: 3000
PORT=3000

# ========================================
# SEGURANÇA - SESSION SECRET
# ========================================
# Chave secreta para criptografar sessões
# ⚠️  CRÍTICO: Use uma chave aleatória forte em produção!
# 
# Para gerar uma chave segura, execute:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 
# Exemplo em desenvolvimento (NÃO USE EM PRODUÇÃO):
SESSION_SECRET=sua_chave_secreta_super_segura_aqui_mude_isto

# ========================================
# CORS - DOMÍNIOS PERMITIDOS
# ========================================
# Lista de domínios que podem acessar a API
# Separados por vírgula, SEM espaços
# 
# Exemplo com múltiplos domínios:
# ALLOWED_ORIGINS=http://localhost:3000,https://seu-dominio.com,https://www.seu-dominio.com
# 
# Em desenvolvimento:
ALLOWED_ORIGINS=http://localhost:3000

# ========================================
# BANCO DE DADOS
# ========================================
# Caminho do arquivo SQLite
DB_PATH=./bolao.db

# ========================================
# LOGGING
# ========================================
# Nível de log: error, warn, info, debug
LOG_LEVEL=info

# ========================================
# CREDENCIAIS ADMIN PADRÃO
# ========================================
# Credenciais usadas na criação inicial do Super Admin
# ⚠️  CRÍTICO: Altere estes valores antes do primeiro deploy!
# Após criar o banco, faça login e altere a senha no painel admin
# 
# Para gerar uma senha forte:
# node -e "console.log(require('crypto').randomBytes(16).toString('base64').replace(/[\/\+=]/g, '') + '@Admin2026!')"
ADMIN_EMAIL=admin@bolao.com
ADMIN_PASSWORD=sua_senha_forte_aqui

# ========================================
# NOTAS IMPORTANTES PARA PRODUÇÃO
# ========================================
# 
# 1. SESSION_SECRET
#    - Deve ser um hash aleatório de 64 caracteres
#    - Use crypto.randomBytes(32).toString('hex')
#    - Mude isto PERIODICAMENTE
#
# 2. ALLOWED_ORIGINS
#    - Adicione seu domínio público
#    - https://seu-dominio.com (com HTTPS!)
#    - Nunca use "*" (aceita qualquer origem)
#
# 3. NODE_ENV
#    - "production" ativa otimizações
#    - Cookies exigem HTTPS (essencial em prod)
#    - Erros não expõem detalhes sensíveis
#
# 4. HTTPS é OBRIGATÓRIO em produção
#    - Use Let's Encrypt (grátis)
#    - Configure proxy reverso (Nginx)
#    - Adicione HSTS headers
#
# 5. Backup do banco
#    - SQLite é um arquivo simples
#    - Faça backup regular de bolao.db
#    - Versione-o fora do código
#
# ========================================
